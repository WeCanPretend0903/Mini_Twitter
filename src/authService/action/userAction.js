export const loginUser = (credentials, history, setSubmitting) => {
     //make checkes and get some data
     const user = {
        userName: "JohnDoe12",
        "email": "jdoe@gmail.com"
     }
     const status = true;

     if (status === true){ 

     } else {

     }

}

export const signupUser = (credentials, history, setSubmitting) => {

}

export const logoutUser = () => {

}